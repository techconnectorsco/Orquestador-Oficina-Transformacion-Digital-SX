// auth.server.ts ya no gestiona login/register con email+password.
// La autenticación se realiza exclusivamente via Microsoft OAuth (Lucia + Arctic).
// Este archivo se mantiene como placeholder para futuras extensiones.
export {};
